export class LoginEntity {
    id ? : number;
    username ? : string;
    password ?: string;

    // constructor (_id : number, _username: string, _password: string) {
    //     this.id = _id;
    //     this.username = _username;
    //     this.password = _password;
    // }
}